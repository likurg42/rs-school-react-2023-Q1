export interface ProfileModelName {
  name: string;
  birthDate: Date;
}

export interface ProfileModel {
  name: string;
  birthDate: Date;
  primaryLanguage: string;
  work: string;
  experience: string;
  avatarUrl: string;
}
