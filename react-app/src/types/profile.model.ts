export type ProfileModel = {
  id: string;
  name: string;
  birthDate: string;
  primaryLanguage: string;
  opensource: boolean;
  experience: string;
  avatarUrl: string | null;
  githubUrl: string;
};
